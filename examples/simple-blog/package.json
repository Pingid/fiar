{
  "name": "simple-blog",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "build:rules": "npx tsx ./rules.ts",
    "deploy:rules": "npx firebase-tools deploy --only storage,firestore:rules",
    "preview": "vite preview",
    "emulate": "npx firebase-tools emulators:start"
  },
  "dependencies": {
    "@firebase/app": "^0.10.1",
    "@firebase/firestore": "^4.6.0",
    "dotenv": "^16.4.5",
    "fiar": "workspace:^"
  },
  "devDependencies": {
    "firebase-tools": "^13.7.2",
    "typescript": "^5.4.5",
    "vite": "^5.2.0"
  }
}
